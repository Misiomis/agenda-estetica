<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Tema oscuro para la pantalla de alarma (siempre oscuro, independiente del sistema) -->
    <style name="Theme.AlarmActivity" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:windowBackground">@color/alarm_bg</item>
        <item name="android:statusBarColor">@color/alarm_bg</item>
        <item name="android:navigationBarColor">@color/alarm_bg</item>
        <item name="colorPrimary">@color/mimart_green_light</item>
        <item name="colorAccent">@color/alarm_dismiss_bg</item>
    </style>
</resources>
